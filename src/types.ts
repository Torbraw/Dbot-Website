export interface NavItem {
  title: string;
  url: string;
}

export interface FeatureItem {
  title: string;
  description: string;
  icon: string;
}

export interface FooterLink {
  url: string;
  description: string;
  icon: string;
}
