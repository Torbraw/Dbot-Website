export type NavItem = {
  title: string;
  url: string;
};

export type FeatureItem = {
  title: string;
  description: string;
  icon: string;
};

export type FooterLink = {
  url: string;
  icon: string;
};
